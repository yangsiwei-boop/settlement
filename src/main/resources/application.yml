server:
  port: 8080
############## Sa-Token ?? (??: https://sa-token.cc) ##############
sa-token:
  # token?? (????cookie??)
  token-name: satoken
  # token??????s ??30?, -1??????
  timeout: 15552000
  # token????? (???????????token??) ??: ?
  active-timeout: -1
  # ???????????? (?true???????, ?false?????????)
  is-concurrent: true
  # ?????????????????token (?true?????????token, ?false?????????token)
  is-share: false
  # token??
  token-style: uuid
  # ????????
  is-log: true
  data-refresh-period: -1
spring:
  application:
    name: settlement
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://14.103.174.114:3306/settlement?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false&rewriteBatchedStatements=true
    username: root
    password: root_PASS866.
    hikari:
      max-lifetime: 29000
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      # ?????
      initialSize: 5
      # ???????
      minIdle: 10
      # ???????
      maxActive: 20
      # ?????????????
      maxWait: 60000
      # ???????????????????????????????
      timeBetweenEvictionRunsMillis: 60000
      # ??????????????????????
      minEvictableIdleTimeMillis: 300000
      # ??????????????????????
      maxEvictableIdleTimeMillis: 900000
      # ??????????
      validationQuery: SELECT 1 FROM DUAL
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
  data: # ??'data'??
    redis:
      database: 0
      host: 14.103.174.114
      port: 6379
      password: '' # ??????????????
      timeout: 10s
      lettuce:
        pool:
          max-active: 200
          max-wait: -1ms
          max-idle: 10
          min-idle: 3
      # ?????Redis?????????????????Spring Cache?
      # cache:
      #   type: redis
      #   redis:
      #     time-to-live: 3600000ms # 1????????
      #     use-key-prefix: true
# application.yml ????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'

knife4j:
  enable: true
  setting:
    language: zh_cn
    enable-footer: false
    enable-footer-custom: true
    footer-custom-content: Studio Settlement API V1.0
